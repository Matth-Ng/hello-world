name: Hotfix Merge and Create Release

on:
  pull_request:
    types: [closed]
    branches:
      - main
  workflow_dispatch:

jobs:

  deploy-to-dev:
    # if: needs.get-source-branch.outputs.SOURCE_BRANCH == 'hotfix'
    uses: ./.github/workflows/cd-build.yml

  delete-current-releases:
    needs: deploy-to-dev
    runs-on: ubuntu-latest
    
    env:
      GITHUB_TOKEN: ${{ github.token }}

    steps:
      - name: Generate token
        id: generate-token
        uses: tibdex/github-app-token@v1
        with:
          app_id: ${{ secrets.ACTIONS_TOKEN_APP_ID }}
          private_key: ${{ secrets.ACTIONS_TOKEN_PRIVATE_KEY }}

      - name: Get releases
        id: get-releases
        uses: octokit/request-action@v2.x
        with:
          route: GET /repos/{repo}/releases
          repo: ${{ github.repository }}

      - name: Get prerelease
        id: get-prerelease
        continue-on-error: true
        uses: sergeysova/jq-action@v2
        with:
          cmd: |
            cat << EOF > json.txt
            ${{ steps.get-releases.outputs.data }}
            EOF
            sed -e "s/'//g" json.txt > newjson.txt
            JSON=$(cat newjson.txt)
            release-id=$(echo "$JSON" | jq -r 'map(select(.published_at and .prerelease == true)) | first | .id')
            release-tag=$(echo "$JSON" | jq -r 'map(select(.published_at and .prerelease == true)) | first | .target_commitish')
            echo "::set-output name=RELEASE_ID::$release-id"
            echo "::set-output name=RELEASE_TAG::$release-tag"
      
      - name: Delete prerelease
        if: steps.get-prerelease.outcome == 'success'
        uses: octokit/request-action@v2.x
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
        with:
          route: DELETE /repos/{repo}/releases/{release_id}
          repo: ${{ github.repository }}
          release_id: ${{ steps.get-prerelease.outputs.RELEASE_ID  }}

      - name: Delete prerelease tag
        if: steps.get-prerelease.outcome == 'success'
        uses: octokit/request-action@v2.x
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
        with:
          route: DELETE /repos/{repo}/git/refs/tags/{ref}"
          repo: ${{ github.repository }}
          reg: ${{ steps.get-prerelease.outputs.RELEASE_TAG  }} 

  delete-current-drafts:
    needs: deploy-to-dev
    runs-on: ubuntu-latest
    
    env:
      GITHUB_TOKEN: ${{ github.token }}

    steps:
      - name: Generate token
        id: generate-token
        uses: tibdex/github-app-token@v1
        with:
          app_id: ${{ secrets.ACTIONS_TOKEN_APP_ID }}
          private_key: ${{ secrets.ACTIONS_TOKEN_PRIVATE_KEY }}

      - name: Get releases
        id: get-releases
        uses: octokit/request-action@v2.x
        with:
          route: GET /repos/{repo}/releases
          repo: ${{ github.repository }}

      - name: Get prerelease
        id: get-prerelease
        continue-on-error: true
        uses: sergeysova/jq-action@v2
        with:
          cmd: |
            cat << EOF > json.txt
            ${{ steps.get-releases.outputs.data }}
            EOF
            sed -e "s/'//g" json.txt > newjson.txt
            JSON=$(cat newjson.txt)
            echo "$JSON"
          # release-id=echo "$JSON" | jq -r 'map(select(.draft == true and .prerelease == true)) | first | .id'
          # release-tag=echo "$JSON" | jq -r 'map(select(.draft == true and .prerelease == true)) | first | .target_commitish'
          # echo "::set-output name=RELEASE_ID::$release-id"
          # echo "::set-output name=RELEASE_TAG::$release-tag"
      
      # - name: Delete prerelease
      #   if: steps.get-prerelease.outcome == 'success'
      #   uses: octokit/request-action@v2.x
      #   env:
      #     GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
      #   with:
      #     route: DELETE /repos/{repo}/releases/{release_id}
      #     repo: ${{ github.repository }}
      #     release_id: ${{ steps.get-prerelease.outputs.RELEASE_ID  }}

      # - name: Delete prerelease tag
      #   if: steps.get-prerelease.outcome == 'success'
      #   uses: octokit/request-action@v2.x
      #   env:
      #     GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}
      #   with:
      #     route: DELETE /repos/{repo}/git/refs/tags/{ref}"
      #     repo: ${{ github.repository }}
      #     reg: ${{ steps.get-prerelease.outputs.RELEASE_TAG  }} 
    
  # create-prerelease:
  #   needs: [delete-current-releases, delete-current-drafts]
  #   uses: ./.github/workflows/cd-create-prerelease-draft.yml
  #   secrets: inherit
  #   with:
  #     disable-workflow: false
  #     disable-notes: false